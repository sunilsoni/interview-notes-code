package com.interview.notes.code.months.march24.test18;

public interface GeographicalArea {
    boolean containsAddress(Address address);
    // Additional methods can be added to handle different attributes.
}