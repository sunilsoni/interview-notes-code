package com.interview.notes.code.misc.imocha;

import org.aspectj.weaver.ast.Or;

import java.util.Arrays;
import java.util.concurrent.CopyOnWriteArrayList;

public class Test1 {

    public static void main(String[] args) {
        //CopyOnWriteArrayList<String> data = new CopyOnWriteArrayList<>(Arrays.asList("a", "B", "D"));
        //new Thread() //Thread 1

        // public void run() data.set(O, nal; data.set(1,"b"); data.set(2, "c"); datc.set(3, "d"); } 1.start(); new Thread() (f/Thread 2 public void run() dotaiset(Or "A"); dataiset(1, "B"); data.set(2, "C"); data.set(3, "D");

//}.start();

//Systentoutprintln(data);


    }
            }
